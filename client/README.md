# GhostRoom Client

Frontend app for GhostRoom built with React + Vite.

Use the root project documentation for full setup and deployment:

- See `../README.md` for complete project instructions.

## Local Development

```bash
cp .env.example .env
npm install
npm run dev
```

## Required Environment Variable

```env
VITE_SERVER_URL=http://localhost:5000
```
